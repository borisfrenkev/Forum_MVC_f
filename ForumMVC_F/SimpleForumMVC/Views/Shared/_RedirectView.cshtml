@model int
<script >

    function Redirect() {
        var redirect = "@Url.Action("Login", "Account", new { returnUrl = Url.Action("Details", "Question", new  {id=@Model }) })";
        if (redirect) {
            window.location.href = redirect;
        }
    }
    Redirect();
</script>
